wir gehen jetzt das Kapitel nach und nach durch, ich schreibt die Änderungswünsche:

Zeile 7: Bitte den Satz umschreiben, keine Administrationsfunktion, eher unser administrationstool.

Es kommt in der Einleitung und in 5.1.1 zu Dopplung von "Verantwortlichkeiten" das liest sich nicht gut.

Zeile 18 sollte einen Verweis auf die tabelle \label{tab:rust-workspace-crates} beinhalten.

Zeile 59, der folgende Satz ist unverständlich. Was soll das heißen, wie kann man das umformulieren? : Die Einordnung als Build-Abhängigkeit ist ein dokumentierter Ersatz für die auf stabilem Cargo nicht verwendbare Artefakt-Abhängigkeit. Diese Umsetzung ergibt sich aus \path{tails-pdp/Cargo.toml} und \path{tails-pdp/build.rs}.


Zeile 94: warum sprechen wir hier auf einmal von attributabhängigen Policies? Sind das nicht Streampolicies? Wo ist der Unterschied?

Zeile 97: ist der typisierte Wert der Attributekey? wenn ja dann bitte deutlich machen.

Zeile 99: Wahrheitswerte bitte mit Boolschen Werten bezeichnen. Warum wird das als u64 gespeichert? Da muss doch nur 0 und 1 rein?

Zeile 144-145: "diese grenze kann auf linuxM systemen die bereitstellung von speicher für ebpfMprogramme und maps beschränken" - Wo ist hier die Quelle? Woher nehmen wir die Information?

Zeile 149: Was ist BTF? Wurde das eingeführt? Quelle? Der Satz "kann ein programm nicht geladen werdenL etwa weil ein hookL btf oder eine benötigte kernelM fähigkeit fehltL wird der start mit einer kontextbezogenen fehlermeldung beendet" ist deswegen auch unverständlich.

Zeile 152: Was ist mit der Debug-Map ? Wo kommt diese her, aus welcher Anforderung ist das? Wofür brauchen wir das?

Zeile 161: "Liefert dagegen eine der asynchronen Aufgaben einen Fehler zurück, wird dieser an das Hauptprogramm weitergegeben und der Prozess beendet." - Wann ist das z.B. der Fall?

Zeile 167: "Beim Start legt der Policyloader, falls erforderlich, das Verzeichnis \path{policies/} im aktuellen Arbeitsverzeichnis an. " kommt raus, bitte Text so schreiben, dass das alles passt.

Zeile 169: Was ist eine Entprellzeit? Wieso ist diese notwendig? Gibt es dafür Quellen? Gibt es dafür ein adneres Wort?

Zeile 179: "für jede file_openMpolicy kontrolliert der loader außerdem die maximale länge der in die festen mapMfelder zu übernehmenden zeichenketten". Was heißt das?

Zeile 181: "Zeichenkettenattribute sind auf Gleichheitsvergleiche beschränkt, weil ihr Wert als Hash vorliegt." Was gibt es noch für Vergleiche bei Zeichenketten?

Zeile 184: Was sind Werttypen?

Zeile 214: was heißt das? "nach der anfänglichen Übernahme beobachtet der attributloader attributes/ rekursiv" ? Kann man das verständlicher schreiben?

Zeile 216: Hier muss ein Verweiß auf die Analogie zum Policyloader rein

Zeile 224: Der Satz "Parsing, Dateisystembeobachtung und die Aufbereitung frei wählbarer Attributnamen verbleiben dagegen im Userspace. Die Aufteilung hält die eBPF-Programme klein und beschränkt ihre Ausführung auf Map-Lookups und Vergleiche fester Datenstrukturen." kann raus, oder? Das ist hier eher unrelevant?

Zeile 227: "Das Einstiegspunktprogramm erhält vom LSM-Hook den aktuellen Prozesskontext und einen Zeiger auf das zu öffnende Kernelobjekt \texttt{file}" gibt es dafür Belege? Quellen? Weiterhin: "Zunächst übernimmt es einen bereits von einem vorherigen BPF-LSM-Programm zurückgegebenen Fehlerwert unverändert." Ist das bei stackable LSMs nicht sowieso der Fall?

Zeile 231: Wäre es nicht eher "Policy evaluieren" und nicht "Policy-Schleife ausführen" ?

Zeile 236: "pro CPU geführten Map" wäre es hier nicht besser, wenn man den originalen Begriff verwendet? Weiterhin: Nicht Policy getroffen, sondern eher sowas wie "Policy wurde nach deny evaluiert" oder so. Bitte entsprechend umschreiben

Zeile 253: Was ist EPERM? Wurde das eingeüführt? Quellen?

Zeile 263: "Daraus entsteht ein konsistenter Auswertungskontext mit den zugehörigen Bänken für alle in diesem Scan betrachteten Deskriptoren." Das muss genauer erklärt werden, das mit den Bänken.

Zeile 274-280: Die Implementierung muss nur für x86_64 erfolgen. Auch die Sache mit dem Cross compile kann komplett raus, MacOS hat hier in der kompletten Arbeit nichts zu suchen, da hier nur für x86_64 entwickelt wird.

Zeile 284-288: Muss das so in dem Umfang rein?

Zeile 306: Das ist unschön formuliert.

Zeile 316: Was ist eine BPF-Laufzeit? Das klingt nach zwangsweise eingedeutscht.

Zeile 319: "panicUnwinding ist in der ebpfMlaufzeit nicht verfügbar" - Woher kommt das? Quelle?

Zeile 321-325: Auch hier: Es muss nur x86_64 berücksichtigt werden. Was heißt x86_64-unknown-linux-musl und aarch64-unknown-linux-musl? Was sind das für Architekturen? Was ist ein Tripel?

Zeile 331: Auch hier nichts von Cross Compile. Weiterhin sollte das mit den Test alles nicht eher im Kapitel Evaluierung vorkommen?

Zeile 334: Hier ist wieder BTF. Siehe Anmerkung zu Zeile 149.


Weiterhin bitte dir Formatierung nochmal anschauen, insbesondere sollte nichts über den Rand hinaus stehen.
Bitte die komplette Arbeit checken, ob sich hier getroffene Entscheidungen auswirken und mitteilen mit Lösungsvorschlägen.