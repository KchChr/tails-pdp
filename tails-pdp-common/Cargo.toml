[package]
name = "tails-pdp-common"
version = "0.1.0"
edition.workspace = true

license.workspace = true

[features]
default = []
user = ["aya", "libc"]

[dependencies]
aya = { workspace = true, optional = true }
libc = { workspace = true, optional = true }

[lib]
path = "src/lib.rs"
