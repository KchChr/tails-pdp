policy "deny socket_bind on 0.0.0.0:8443 from 16 UTC onwards"
deny
    action == "socket_bind";
    resource.family == "inet";
    resource.ip == "0.0.0.0";
    resource.port == 8443;
    environment.utc.hour >= 16;
